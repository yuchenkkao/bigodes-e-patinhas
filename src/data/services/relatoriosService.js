/** @typedef {import('../@types/Relatorio').RelatorioMensal} RelatorioMensal */

/** @param {string} mes @returns {Promise<RelatorioMensal|null>} */
export async function buscarRelatorio(mes) {
  return null;
}
